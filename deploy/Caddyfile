# 서버의 /etc/caddy/Caddyfile 내용. 이름은 "공인IP를 하이픈으로 바꾼 값 + .sslip.io"
# (sslip.io는 이 이름을 그 IP로 되돌려주는 무료 DNS라 도메인을 살 필요가 없다).
# Caddy가 Let's Encrypt 인증서를 자동으로 받고 갱신한다 (80/443 포트가 열려 있어야 함).
150-230-211-135.sslip.io {
	reverse_proxy localhost:3000
}
